# 忽略 PyCharm IDE 的配置文件
# 这个文件夹包含了您个人的项目布局、窗口设置等，不应共享。
.idea/

# 忽略 Python 虚拟环境
# 这个文件夹体积巨大，且包含与您本机相关的包，其他人无法直接使用。
# 团队成员应根据 requirements.txt 自行创建虚拟环境。
.venv/

# 忽略 Python 编译产生的字节码缓存
# 这些文件由 Python 自动生成，用于加速模块加载，无需版本控制。
__pycache__/
*.pyc

# 忽略打包工具 (PyInstaller) 生成的文件和文件夹
# 这些是编译过程的中间文件和最终产物，不属于源代码。
# 通常我们只管理源代码，最终的 .exe 文件应在 GitHub Releases 页面发布。
/build/
/dist/
*.spec

# 忽略包含私密信息（如API Key）的配置文件
# 这是最重要的规则之一，防止密钥泄露！
config.ini

# 忽略用户自定义的归档文件夹（可选）
# 如果您不希望将归档文件夹上传到仓库，请取消下面一行的注释。
/archive/

# 忽略其他常见的临时文件或日志文件
*.log
.env